import {PluginId} from './plugin_id';

export default {
    CLOSE_BULK_INVITE_CHANNEL_MODAL: `${PluginId}_close_bulk_invite_channel_modal`,
    OPEN_BULK_INVITE_CHANNEL_MODAL: `${PluginId}_open_bulk_invite_channel_modal`,
};
