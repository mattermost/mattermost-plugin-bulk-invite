import {manifest} from './manifest';

export default {
    CLOSE_BULK_ADD_CHANNEL_MODAL: `${manifest.id}_close_bulk_invite_channel_modal`,
    OPEN_BULK_ADD_CHANNEL_MODAL: `${manifest.id}_open_bulk_invite_channel_modal`,
};
